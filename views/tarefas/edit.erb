<div>
  <div class="col-md-8 col-md-offset-2">
    <form class="" action="/tarefas/<%= @tarefa.id %>" method="post">
      <input name="_method" type="hidden" value="put" />
      <div class="form-group">
        <input type="text" name="tarefa[content]" value="<%= @tarefa.content %>" id="" class="form-control" />
      </div>
      <div class="form-group">
        <textarea name="tarefa[details]" rows="3" cols="40"><%= @tarefa.details %></textarea></p>
      </div>
      <button type="submit" class="btn btn-success pull-right">Salvar alterações <span class="glyphicon glyphicon-chevron-right" /></button>
    </form>
  </div>
</div>
